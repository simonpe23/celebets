// Inter Tight is the numeral face. Used for money and for counts, at
// any size, so a number always looks like a number. Text stays Geist.
//
// No negative letter spacing here: that belongs on the big hero
// numbers only, where the default spacing looks loose.
export const MONEY_FONT = {
  fontFamily: "var(--font-inter-tight)",
} as const;
